import { ProgramtermPipe } from './programterm.pipe';

describe('ProgramtermPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgramtermPipe();
    expect(pipe).toBeTruthy();
  });
});
